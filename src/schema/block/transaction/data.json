{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schema/block/transaction/data.json",

  "type": "object",
  "properties": {
    "seq": {
      "type": "integer",
      "minimum": 1
    },
    "command": {
      "type": "string",
      "pattern": "^data$"
    },
    "ns": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9:._-]{1,126}[A-Za-z0-9]$"
    },
    "base64url": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{1,8192}$"
    }
  },
  "additionalProperties": false,
  "required": [
    "seq",
    "command",
    "ns",
    "base64url"
  ]
}