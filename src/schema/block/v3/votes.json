{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schema/block/v3/votes.json",

  "type": "object",
  "properties": {
    "origin": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{43}$"
    },
    "sig": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{86}$"
    }
  },
  "additionalProperties": false,
  "required": [
    "origin",
    "sig"
  ]
}