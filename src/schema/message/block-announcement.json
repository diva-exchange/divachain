{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "/schema/message/block-announcement.json",

  "type": "object",
  "properties": {
    "e": {
      "type": "integer",
      "minimum": 1
    },
    "ha": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{43}$"
    },
    "sig": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{86}$"
    }
  },
  "required": [
    "e",
    "ha",
    "sig"
  ],
  "additionalProperties": false
}
