{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schema/message/message.json",

  "type": "object",
  "properties": {
    "ident": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{1,160}$"
    },
    "type": {
      "type": "integer",
      "minimum": 1,
      "maximum": 9
    },
    "data": {
      "oneOf": [
        {
          "$ref": "auth.json"
        },
        {
          "$ref": "challenge.json"
        },
        {
          "$ref": "tx-proposal.json"
        },
        {
          "$ref": "vote.json"
        },
        {
          "$ref": "sync.json"
        }
      ]
    },
    "broadcast": {
      "type": "boolean"
    },
    "trail": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-f0-9]{64}$"
      },
      "minItems": 1,
      "uniqueItems": true
    }
  },
  "required": [
    "ident",
    "type",
    "data",
    "broadcast"
  ],
  "additionalProperties": false
}