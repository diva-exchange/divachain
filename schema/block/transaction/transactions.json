{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schema/block/transaction/transactions.json",

  "type": "object",
  "properties": {
    "origin": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{43}$"
    },
    "commands": {
      "anyOf": [
        {
          "$ref": "/schema/block/transaction/addPeer.json"
        },
        {
          "$ref": "/schema/block/transaction/removePeer.json"
        }
      ]
    },
    "sig": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{86}$"
    }
  }
}